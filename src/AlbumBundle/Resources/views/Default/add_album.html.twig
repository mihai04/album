{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_4_horizontal_layout.html.twig' %}

{% block body %}
    <h3>Add You Favourite Album</h3>
{#    {{ form(form) }}#}


    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Safe</button>
    {{ form_end(form) }}
{% endblock %}